import React from 'react';
import { Container } from './styles';

interface Props {

}

const Item: React.FC<Props> = (props) => {
  const { } = props;

  return (
    <Container>
      Card
    </Container>
  );
};

export default Item;
