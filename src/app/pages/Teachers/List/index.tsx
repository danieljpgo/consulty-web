import React from 'react';
import { Container } from './styles';

interface Props {

}

const List: React.FC<Props> = (props) => {
  const { } = props;

  return (
    <Container>
      a
    </Container>
  );
};

export default List;
